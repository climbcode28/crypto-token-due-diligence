@AGENTS.md

Claude Code specifics: the EVM skill's Claude Code copy is
`.claude/skills/crypto-evm-token-due-diligence` (port notes in its `CLAUDE-CODE-PORT.md`) and its
two research-lane subagents live in `.claude/agents/`. Everything imported above applies unchanged.
